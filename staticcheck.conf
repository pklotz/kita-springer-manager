# Disabled style checks (the "ST" prefix is style, not correctness):
#   ST1000 — at least one file per package needs a doc comment. Pedantic for
#            our small leaf packages whose names are self-describing.
#   ST1005 — error strings should not be capitalised. English-only convention;
#            our user-facing messages are German and read as proper sentences.
checks = ["all", "-ST1000", "-ST1005"]
